# Copy this file to .env and fill in the values you need.
# .env is git-ignored — never commit real keys.

# --- Node 5 generation (pick one; ANTHROPIC_API_KEY takes priority) ---
ANTHROPIC_API_KEY=
GROQ_API_KEY=

# --- Node 1 data sources (optional) ---
NVD_API_KEY=

# --- Node 8 deployment (optional overrides) ---
# API_HOST=0.0.0.0
# API_PORT=8001
