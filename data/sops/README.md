# Internal SOPs

Drop your organization's incident-response, vulnerability-management, and
security-operations procedures here (`.md`, `.txt`, `.pdf`, or `.docx`).

`InternalSopSource` (in `src/data_sources/internal_sop.py`) recursively loads
every file in this directory as a `RawDocument` for the RAG pipeline.
